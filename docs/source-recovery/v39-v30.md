# WhiteRoom 游戏源码恢复记录（v39 / v30）

## 来源

- 源档案：`Altverse-WhiteRoom-source-v39-v30-20260723.zip`
- 档案 SHA-256：`09b43c53b9d076d09650d9dabedd684ebd5dd6b3956800c0f7b1fa3344e6a717`
- Source runtime：`runtime-20260721-v39-v30-metropolitan-museum`
- Game release：`20260721-whiteroom-ugc-v39-metropolitan-museum-gallery`
- Platform release：`20260721-whiteroom-platform-v30-metropolitan-museum-gallery`
- Game source SHA-256：`249c45d236fbdd6dc360bbcfed17fbe7e5983b70e21b1ea733ed434df27ec0dc`

导入前已逐项校验档案内 `SHA256SUMS` 的 194 个条目。三个中文文档文件名因 ZIP 编码标志缺失在 Windows API 中显示为乱码，但内容哈希与清单完全一致；导入的 `whiteroom/` 路径均为 ASCII，不受此问题影响。

## 导入范围

仅将档案内的 `whiteroom/` 导入为 `apps/game/`，共 99 个文件。未导入同档案中的平台、Worker、基础设施和产品文档副本，避免覆盖仓库中更新的服务端实现。

源码与现有 `public/game/assets/index-*.js` 共享关键运行时标记，契约测试会持续验证这条可读源码血缘。

## 当前差距

这份源码对应 2026-07-21 的生产基线，而仓库 `main` 在此后还有部署、OTP 等增量。因此本次只恢复可维护源码与构建链，不直接用新构建覆盖 `public/game/`。

正式切换生产入口前仍需：

1. 盘点并回迁 2026-07-21 之后对压缩成品做过的前端增量。
2. 比较 `apps/game/dist/` 与当前 `public/game/` 的路由、API、资产和运行时行为。
3. 完成关键流程浏览器试玩、性能检查与发布回滚演练。
4. 在独立发布提交中由构建产物受控更新 `public/game/`。

## 为什么要保留源码工程

压缩 bundle 适合浏览器执行，不适合长期开发。恢复 `apps/game/` 后，可以进行类型检查、单元测试、模块级修改、依赖管理、源码审查和可重复构建；同时把生产切换留给独立验收步骤，避免一次导入同时改变线上行为。
